<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RadioGroup
        android:id="@+id/radioGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <CheckBox
            android:id="@+id/cBisSyncedFilter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Filter only synced" />

        <CheckBox
            android:id="@+id/cBisUnsyncedOnlyLocalFilter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Filter only local" />

        <CheckBox
            android:id="@+id/cBisUnsyncedOnlyRemoteFilter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Filter only remote" />

        <CheckBox
            android:id="@+id/cBisFirstVersionFilter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Filter only first versions" />

        <CheckBox
            android:id="@+id/cBisFileFilter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Filter only files" />

        <CheckBox
            android:id="@+id/cBisDirectoryFilter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Filter only directories" />

    </RadioGroup>

</androidx.constraintlayout.widget.ConstraintLayout>